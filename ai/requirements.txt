fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.12.5
psycopg[binary]==3.2.3
python-dotenv==1.0.1
openai>=2.36.0
openai-agents[realtime]==0.17.2
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0
tiktoken>=0.8.0
pgvector==0.3.6
websockets>=13.0
boto3>=1.35.0

# --- LangGraph stack (new agent runtime) -----------------------------------
langgraph>=0.6.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
langsmith>=0.2.0

# --- LiveKit Agents (voice pipeline: STT/LLM/TTS plumbing) -----------------
# Plugins are used standalone — no LiveKit Room required. Audio I/O
# rides the existing FastAPI WebSocket bridges (browser PCM16, Twilio mulaw).
livekit-agents[langchain,deepgram,cartesia,silero]>=1.5.0

# audioop is removed in Python 3.13 — provide a shim for the Twilio bridge.
audioop-lts; python_version >= "3.13"
