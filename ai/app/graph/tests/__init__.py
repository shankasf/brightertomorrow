"""Self-contained import + planner + graph compile tests.

Run with:

    python -m app.graph.tests.smoke

No API keys required. Mocks the LLMs so the graph can be invoked
end-to-end against synthetic input.
"""
from __future__ import annotations
